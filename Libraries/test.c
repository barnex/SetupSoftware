#include "io.h"

int testmain(int argc, char**argv) {
	setProgName(argv[0]);

	//int badfd = 77;
	//ewrite(badfd, "hello", 5);

//	FILE * pFile;
//   char mystring [100];
//
//   pFile = fopen ("myfile.txt" , "w");
//   if (pFile == NULL) perror ("Error opening file");
//     efgets (mystring , 100 , pFile);
//       puts (mystring);


	return 0;
}
